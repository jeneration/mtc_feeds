<?php
/**
 * @file
 * Bulk export of feeds_importer_default objects generated by Bulk export module.
 */

/**
 * Implements hook_feeds_importer_default().
 */
function mtc_feeds_feeds_importer_default() {
  $path = drupal_get_path('module', 'mtc_feeds') . '/importers';
  $files = file_scan_directory($path, '/\.inc$/');

  $importers = array();

  foreach ($files as $filepath => $file) {
    require $filepath;
    if (isset($feeds_importer)) {
      $importers[$feeds_importer->id] = $feeds_importer;
    }
  }

  return $importers;
}
